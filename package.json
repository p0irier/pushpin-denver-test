{
  "name": "pushpin-denver-test",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "fetch:ski": "node scripts/fetch-ski.js",
    "fetch:mtb": "node scripts/fetch-mtb.js",
    "fetch:breweries": "node scripts/fetch-breweries.js",
    "fetch:all": "npm run fetch:ski && npm run fetch:mtb && npm run fetch:breweries"
  }
}
